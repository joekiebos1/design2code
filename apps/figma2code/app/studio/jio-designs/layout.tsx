import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Jio Designs',
}

export default function JioDesignsLayout({ children }: { children: React.ReactNode }) {
  return children
}
